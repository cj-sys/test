상수 필드: 변수를 바꿀수 없는 필드 STATIC FINAL
STATIC FINAL는 객체마다 저장되지 않고, 클래스에만 포함도니다.

왜 우리는 인터페이스 생성시 상수필드를 직접 선언하지 않았는가?
변수를 생성과 동시에 초기화를 해주기 때문에

A INSTANCEOF  B   B가 더 큰범위

다형성: 부모 클래스에 정의 된 매소드를 어떤 클래스가 상속 받느냐에 따라 메서드의 행위는 같을 수 있으나
	실제 구현되는 내부 구조는 다를 수 있다. 오버로딩과 오버라이딩이 다형성의 특징중 하나이다.

27페이지 위로 올리기

업케스팅 아래부분 질문